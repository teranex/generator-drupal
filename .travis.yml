language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-drupal-project' ]; then cd .. && eval "mv $currentfolder generator-drupal-project" && cd generator-drupal-project; fi

