core = 7.x
api = 2

; defaults
defaults[projects][subdir] = "contrib"

; Drupal core
projects[] = "drupal"

; contrib modules
projects[] = ctools
projects[] = features
projects[] = pathauto
projects[] = strongarm
projects[] = token
projects[] = views

; contrib themes

; libraries
